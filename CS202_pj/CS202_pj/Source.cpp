#include"Library.h"
//start x,y CDINAUSOR is (4,3)
//for testing purpose

mutex CGame::mtx;
int main()
{
	ShowConsoleCursor(false);
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	CGame game;

	return 0;
}